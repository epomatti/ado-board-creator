# ado-board-creator

Script to create Azure DevOps team

```
python3 -m venv env
. env/bin/activate
pip install -r requirements.txt
```

Config the project:

```sh
# Enter the required values
cp example.env .env
```

Run it:

```sh
python3 src/main.py
```